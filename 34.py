#split string

# the split()  method returns a list where the text between
# the specifies sperator becomes the list items.


# the split() method splits the string into substrings

# if it finds instances of the separator:


a = "Hello, World!"
print(a.split(","))