# loop through the index numbers

# you can also loop through the tuple items by refferring
# to their index numbers




thistuple = ("apple", "banana", "cherry")
for i in range(len(thistuple)):
    print(thistuple[i])