#get items

# the items() method will return each item in a dictionary,
# as tuples in a list.

# get a list of the key: value pairs ?

thisdict = {
    "brand": "Ford",
    "model": "mustang",
    "year": 1964
}

x = thisdict.items()
print(x)