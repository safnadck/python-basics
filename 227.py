# Access set items

#you cannot access items in a set by referrring to an index or a key

# but you can loop through the set items using a for loop,
# or ask if a specified value is presented in a set by  using the in keyword


thisset = {"apple", "banana", "cherry"}
for x in thisset:
    print(x)